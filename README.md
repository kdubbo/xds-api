# Dubbo xDS API

This repository defines the xDS protocol models and gRPC service interfaces required for communication between the Dubbo control plane and data plane. Most of these definitions are implemented using [protobuf](https://github.com/google/protobuf).

The gRPC code in this repository defines the xDS service interfaces and also includes client connection handling and xDS address resolution.





